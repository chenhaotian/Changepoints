## plot contour lines on discriminant boundaries

## when shape = 0, xspline() is the same as lines()
contour <-xspline(list(x=numeric(),y=numeric()),open = TRUE,draw = FALSE,shape = 1)

density

library(MASS)
kde2d

